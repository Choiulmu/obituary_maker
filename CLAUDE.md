# obituary_maker

## 작업 규칙

- 확인 없이 진행할 것. 코드 작성/수정/삭제, 파일 생성, 빌드, 테스트, 로컬 실행 등은 묻지 말고 바로 한다.
- 예외 — 아래는 실행 전에 반드시 묻는다:
  - DB 직접 접근 (직접 쿼리 실행, 스키마 변경, 데이터 수정/삭제)
  - 인프라 직접 수정 (배포, 서버/클라우드 설정 변경, CI/CD 파이프라인 변경, 시크릿 변경)

---

## 1. 프로젝트 개요

어르신들이 복잡한 절차 없이 몇 가지 정보만 입력하면 **온라인 부고장을 생성하고 링크를 카카오톡 등으로 쉽게 공유할 수 있도록 하는 서비스**다.

서비스 이용자는 회원가입이나 로그인 없이 부고장 정보를 입력하고, 생성된 부고장 링크를 공유할 수 있다.

운영자는 고객이 입력한 부고장 정보를 확인하고 관리할 수 있다.

---

## 2. 기술 스택

### Backend

* Java
* Spring Boot
* Google Sheets

Google Sheets를 초기 데이터 저장소로 사용한다.

MVP에서는 별도의 PostgreSQL 등의 데이터베이스를 구축하지 않고, Google Sheets를 데이터베이스처럼 활용한다.

### Frontend

* Thymeleaf
* HTML / CSS
* 필요한 경우 최소한의 JavaScript 사용

Thymeleaf를 주된 서버 사이드 렌더링 기술로 사용한다.

MVP에서는 React, Vue 등의 별도 SPA 프레임워크를 도입하지 않는다.

### 기본 구조

Spring Boot + Thymeleaf 기반의 단순한 웹 애플리케이션을 지향한다.

```text
Client
   ↓
Spring Boot
   ├── 부고장 생성
   ├── 부고장 조회
   ├── Google Sheets 저장/조회
   └── 운영자 알림
          ↓
     Google Sheets
```

프론트엔드와 백엔드를 불필요하게 분리하지 않고, Thymeleaf를 활용하여 서버에서 HTML을 렌더링하는 방식을 우선한다.

---

## 3. MVP 핵심 기능

### 고객

* 부고장 생성 페이지 접속
* 고인 및 장례 정보 입력
* 부고장 미리보기
* 부고장 생성
* 고유 링크 발급
* 생성된 부고장 링크 공유

### 운영자

* 새로운 부고장 생성 알림
* 고객이 입력한 정보 확인
* 부고장 링크 확인
* 입력 데이터를 Google Sheets로 관리

---

## 4. 고객 입력 정보

### 필수 정보

* 고인 성함
* 별세일
* 장례식장 / 빈소(호실)
* 발인일
* 상주 이름
* 상주 연락처

### 선택 정보

* 장례식장 주소
* 오시는 길
* 조의금 계좌
* 추가 안내 문구

---

## 5. 고객 Flow

```text
사이트 접속
   ↓
[부고장 만들기]
   ↓
부고 정보 입력
   ↓
부고장 미리보기
   ↓
내용 확인
   ↓
[부고장 만들기]
   ↓
부고장 생성
   ↓
고유 링크 발급
   ↓
[링크 공유하기]
```

입력부터 생성까지의 흐름을 최대한 단순하게 유지한다.

---

## 6. 부고장 생성 후 처리

부고장이 생성되면 다음 작업을 수행한다.

1. 고객에게 생성된 부고장 페이지를 보여준다.
2. 부고장에 접근할 수 있는 고유 URL을 발급한다.
3. 고객이 링크를 복사하거나 카카오톡으로 공유할 수 있도록 한다.
4. 운영자에게 새로운 부고장이 생성되었다는 알림을 보낸다.
5. 입력된 정보를 Google Sheets에 기록한다.

---

## 7. 부고장 페이지

모바일 환경을 기준으로 설계한다.

기본적으로 다음 정보를 제공한다.

* 고인 성함
* 별세 안내 문구
* 빈소
* 장례식장
* 발인일
* 상주
* 장례식장 위치
* 조의금 계좌
* 공유하기

---

## 8. UX 원칙

주 사용자가 컴퓨터나 스마트폰 사용에 익숙하지 않은 어르신이라는 점을 항상 고려한다.

* 큰 글씨를 사용한다.
* 버튼을 크게 만든다.
* 입력 항목을 최대한 단순하게 구성한다.
* 어려운 용어 사용을 지양한다.
* 모바일 화면을 우선한다.
* 입력 → 확인 → 생성의 단순한 흐름을 유지한다.
* 사용자가 다음에 무엇을 해야 하는지 명확하게 알 수 있도록 한다.
* 불필요한 화면 이동과 입력 단계를 만들지 않는다.
* 한 화면에 너무 많은 정보를 요구하지 않는다.
* 에러 메시지는 사용자가 이해하기 쉬운 표현으로 작성한다.

**목표는 컴퓨터나 스마트폰 사용에 익숙하지 않은 사람도 별도의 도움 없이 쉽게 부고장을 만들 수 있도록 하는 것이다.**

---

## 9. MVP에서 제외하는 기능

초기 MVP에서는 다음 기능을 구현하지 않는다.

* 회원가입 / 로그인
* 결제
* 다양한 부고장 디자인
* 방명록
* 통계
* 앱
* 다국어
* 문자 발송

서비스 사용성을 확인한 이후 실제 필요성이 확인된 기능을 추가한다.

---

## 10. 데이터 관리

초기에는 별도의 데이터베이스를 구축하지 않고 **Google Sheets 기반으로 데이터를 관리한다.**

기본적인 데이터 흐름은 다음과 같다.

```text
고객 입력
   ↓
Spring Boot
   ├── 부고장 생성
   ├── 고유 링크 생성
   ├── 운영자 알림
   └── Google Sheets 기록
```

Google Sheets를 초기 데이터 저장소로 사용하므로 MVP에서는 PostgreSQL 등의 별도 DB를 도입하지 않는다.

서비스 이용자가 증가하거나 데이터 관리가 복잡해지는 등 실제 필요성이 발생하면 PostgreSQL 등의 데이터베이스로 전환한다.

DB 전환 가능성만을 이유로 MVP 단계에서 과도한 추상화나 복잡한 데이터 계층을 만들지 않는다.

---

## 11. 개발 원칙

### 11.1 단순한 구조 우선

복잡한 구조보다 이해하기 쉬운 구조를 우선한다.

현재 요구사항을 가장 단순하고 명확한 방법으로 해결한다.

아직 요구되지 않은 기능이나 미래의 확장 가능성을 위해 복잡한 구조를 미리 만들지 않는다.

다음과 같은 과도한 설계를 지양한다.

* 사용하지 않는 기능을 위한 코드
* 과도한 설정
* 불필요한 디자인 패턴 적용
* premature optimization
* 실제 요구사항이 없는 추상화
* 미래의 변경 가능성만 고려한 과도한 계층 분리

---

### 11.2 불필요한 주석 작성 금지

코드를 보면 누구나 이해할 수 있는 내용은 주석으로 작성하지 않는다.

```java
// 사용자 이름을 가져온다.
String userName = user.getName();
```

위와 같은 주석은 작성하지 않는다.

주석은 코드만으로 의도를 이해하기 어렵거나 다음과 같은 내용을 설명해야 하는 경우에만 작성한다.

* 복잡한 비즈니스 규칙
* 외부 시스템의 특수한 제약
* 의도적으로 일반적인 방식과 다르게 구현한 이유
* 향후 변경 시 주의해야 하는 사항

주석을 작성하기 전에 코드를 더 명확하게 작성할 수 있는지 먼저 확인한다.

---

### 11.3 명확한 네이밍

변수명은 명사형을 사용한다.

메서드명은 동사형을 사용한다.

예:

```java
String userName;
Funeral funeral;
CondolenceAccount condolenceAccount;
```

```java
createFuneral();
saveFuneral();
findFuneral();
sendNotification();
```

이름만 보더라도 역할과 책임을 이해할 수 있도록 작성한다.

축약어나 의미가 불명확한 이름을 지양한다.

---

### 11.4 객체지향 설계 지향

객체지향 원칙을 지향하고 각 객체가 명확한 책임을 가지도록 설계한다.

단, 객체지향이라는 이유만으로 불필요한 추상화나 계층을 추가하지 않는다.

다음과 같은 구조를 무조건적으로 만들지 않는다.

```text
Controller
  ↓
Facade
  ↓
Service
  ↓
Manager
  ↓
UseCase
  ↓
Handler
  ↓
Repository
  ↓
DAO
```

실제 요구사항과 책임에 따라 필요한 수준으로만 분리한다.

---

### 11.5 DTO 및 클래스 생성

불필요한 DTO나 클래스를 무분별하게 생성하지 않는다.

새로운 DTO나 클래스를 생성하기 전에 다음을 먼저 확인한다.

1. 기존 객체로 처리할 수 있는가?
2. 새로운 책임이 실제로 필요한가?
3. 객체를 분리했을 때 코드의 이해와 유지보수가 좋아지는가?
4. 단순히 데이터를 옮기기 위한 목적은 아닌가?
5. 기존 클래스의 책임이 과도하게 커진 것이 아니라면 굳이 분리할 필요가 없는가?

분리가 필요한 명확한 이유가 있을 때만 새로운 DTO나 클래스를 생성한다.

특히 단순히 Controller → Service → Repository 사이에서 데이터를 전달하기 위한 DTO를 관습적으로 생성하지 않는다.

---

### 11.6 계층 분리

Spring의 Controller, Service 등의 계층을 사용하되 형식적으로 모든 기능을 동일한 구조로 만들지 않는다.

각 계층에는 실제 책임이 있어야 한다.

* Controller: HTTP 요청/응답과 사용자 입력 처리
* Service: 필요한 경우 비즈니스 로직 처리
* Google Sheets 연동 코드: 실제 Sheets API와의 통신 및 데이터 처리

단순한 기능을 계층을 늘려서 복잡하게 만들지 않는다.

---

### 11.7 MVP 우선

현재 필요한 기능을 먼저 구현한다.

"나중에 필요할 수도 있다"는 이유만으로 기능이나 구조를 미리 추가하지 않는다.

MVP에서 중요한 것은 완벽한 확장성보다 **실제 사용자가 부고장을 쉽게 생성할 수 있는지 확인하는 것**이다.

---

## 12. 개발 시 작업 원칙

코드를 수정하거나 새로운 기능을 구현할 때 다음 원칙을 따른다.

1. 먼저 기존 코드를 확인한다.
2. 기존 프로젝트의 구조와 패턴을 우선적으로 따른다.
3. 기존 코드로 해결할 수 있는지 먼저 확인한다.
4. 요구사항에 없는 기능을 임의로 추가하지 않는다.
5. 가장 작은 변경으로 문제를 해결한다.
6. 관련 없는 영역까지 함께 리팩터링하지 않는다.
7. 중복 코드를 만들지 않는다.
8. 불필요한 클래스와 DTO를 추가하지 않는다.
9. 코드만으로 이해할 수 있는 주석을 작성하지 않는다.
10. 필요한 경우 테스트를 작성한다.
11. 사용자에게 노출되는 문구는 어르신도 쉽게 이해할 수 있는 표현을 사용한다.

---

## 13. Thymeleaf 개발 원칙

Thymeleaf를 주된 화면 렌더링 방식으로 사용한다.

* 서버 사이드 렌더링을 기본으로 한다.
* 복잡한 JavaScript 기반 UI를 기본 방식으로 사용하지 않는다.
* 단순한 동작은 HTML과 Thymeleaf로 해결한다.
* JavaScript는 실제로 필요한 경우에만 사용한다.
* 모바일 화면을 우선하여 HTML/CSS를 작성한다.
* 폼 입력과 검증 과정은 사용자가 이해하기 쉽게 구성한다.

---

## 14. Google Sheets 개발 원칙

Google Sheets는 MVP의 데이터 저장소 역할을 한다.

Google Sheets 연동을 구현할 때도 실제 요구사항에 필요한 수준으로만 구성한다.

* 부고장 생성 정보 저장
* 필요한 부고장 정보 조회
* 운영자가 확인할 수 있는 형태로 데이터 관리

Google Sheets를 사용하는 것 자체를 이유로 복잡한 Repository, DAO 등의 추상화 계층을 만들지 않는다.

추후 PostgreSQL 등으로 전환할 때 실제 전환 요구사항에 맞춰 구조를 개선한다.

---

## 15. 사용자 관점의 최우선 원칙

모든 기능 구현과 UI 결정에서 **사용자가 쉽게 사용할 수 있는가**를 가장 중요하게 고려한다.

특히 부고장은 긴급한 상황에서 사용될 수 있으므로 사용자가 기능을 이해하기 위해 별도의 학습을 해야 하는 구조를 만들지 않는다.

기능을 추가할 때는 다음을 확인한다.

* 사용자가 이 기능의 목적을 바로 이해할 수 있는가?
* 버튼의 의미를 바로 이해할 수 있는가?
* 다음 단계가 명확한가?
* 입력해야 할 정보가 최소화되어 있는가?
* 모바일에서 사용하기 편한가?
* 오류가 발생했을 때 어떻게 해결해야 하는지 알 수 있는가?

**기술적으로 좋은 구조보다 실제 사용자가 쉽게 부고장을 만들 수 있는 서비스를 만드는 것을 우선한다.**
